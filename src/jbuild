(library
 ((name async_kernel)
  (public_name async_kernel)
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane -annotated-ignores -ite-check -check-doc-comments)))
))


(jbuild_version jane_street)
