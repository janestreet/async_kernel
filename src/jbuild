(library
 ((name async_kernel)
  (inline_tests ((javascript build_and_run)))
  (public_name async_kernel)
  (libraries (core_kernel))
  (preprocess (pps (ppx_jane.kernel -annotated-ignores -ite-check -check-doc-comments)))
  (js_of_ocaml ())))

(enforce_style ())
